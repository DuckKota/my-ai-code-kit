image: python:3.11

before_script:
  - pip install -r requirements.txt

test:
  script:
    - pytest tests/ -q
    - mypy src/
    - pylint src/
    - python -c "import sys; sys.path.insert(0,'src'); import ocmanifest; ocmanifest.load('manifest.toml'); print('manifest OK')"
